language: php

php:
  - 5.3.3
  - 5.3
  - 5.4
  - 5.5

install: ./.travis.init
script:  ./vendor/bin/ict build

# PHP 5.5 is still in alpha, so ignore build failures.
matrix:
  allow_failures:
    - php: 5.5
