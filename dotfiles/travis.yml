language: php

php:
  - 5.3.3
  - 5.3
  - 5.4
  - 5.5

env:
  global:
    {env}

matrix:
  allow_failures:
    - php: 5.5

install:        composer install --dev
script:         ./vendor/bin/travis-build
after_success:  ./vendor/bin/travis-success
