language: php

php:
  - 5.3.3
  - 5.3
  - 5.4
  - 5.5

env:
  global:
    - secure: "ICECAVE_TOKEN=<token>"

matrix:
  allow_failures:
    - php: 5.5

install:        composer install --dev
script:         ./vendor/bin/phpunit-coverage
after_success:  ./vendor/bin/publish-coverage-reports
