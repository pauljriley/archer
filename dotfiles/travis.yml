language: php

php:
  - 5.3.3
  - 5.3
  - 5.4
  - 5.5

matrix:
  allow_failures:
    - php: 5.5
  include:
    - php: 5.4
      env:
        secret: "encrypted ICECAVE_TOKEN=..."

before_script:
  - ./vendor/bin/travis-before

script:
  - ./vendor/bin/travis-build

after_success:
  - ./vendor/bin/travis-after-success

after_failure:
  - ./vendor/bin/travis-after-failure
