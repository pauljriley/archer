language: php

php:
  - 5.3.3
  - 5.3
  - 5.4
  - 5.5

matrix:
  allow_failures:
    - php: 5.5
  include:
    - php: 5.4
      env:
        - secret: "encrypted ICECAVE_TOKEN=..."

before_script: composer install --dev

script: ./vendor/bin/phpunit-coverage

after_success: ./vendor/bin/publish-coverage-reports
